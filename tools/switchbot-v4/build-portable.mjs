import {build} from 'esbuild';
import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const root=path.resolve(process.argv[2]||'switchbot-living-world-v4');const output=path.resolve(process.argv[3]||'portable');fs.mkdirSync(output,{recursive:true});
const html=fs.readFileSync(path.join(root,'index.html'),'utf8'),css=fs.readFileSync(path.join(root,'src/styles.css'),'utf8');
const result=await build({entryPoints:[path.join(root,'src/app.js')],bundle:true,write:false,format:'iife',platform:'browser',target:'es2022',minify:true,legalComments:'inline',plugins:[{name:'vendored-three',setup(b){b.onResolve({filter:/^three$/},()=>({path:path.join(root,'vendor/three/three.module.min.js')}));b.onResolve({filter:/^three\/addons\//},args=>({path:path.join(root,'vendor/three/addons',args.path.slice('three/addons/'.length))}));}}]});
let js=result.outputFiles[0].text;
// The live shared URL is meaningful only after hosting. Local delivery shares the portable plan file.
js+='\nif(location.protocol===\'file:\'){document.querySelector(\'#sharePlan\').textContent=\'プランを共有（ファイル） ↗\';document.querySelector(\'#sharePlan\').onclick=()=>document.querySelector(\'#exportPlan\').click();}';
const facts=fs.readFileSync(path.join(root,'docs/FACTS.html'),'utf8');const factBody=facts.slice(facts.indexOf('<h1>')).replace(/<\/html>\s*$/,'').replace('href="../vendor/three/LICENSE"','href="#three-license"');
const license=fs.readFileSync(path.join(root,'vendor/three/LICENSE'),'utf8').replaceAll('&','&amp;').replaceAll('<','&lt;');
const info=`<section id="facts-inline" style="padding:40px max(24px,6vw);max-width:1100px;margin:auto;line-height:1.8"><details><summary style="cursor:pointer;padding:20px">機能の出典・対応条件・ライセンス</summary>${factBody}<pre id="three-license" style="white-space:pre-wrap;font-size:11px">${license}</pre></details></section>`;
let single=html.replace('<link rel="stylesheet" href="./src/styles.css">',()=>'<style>'+css+'</style>').replace(/<script type="importmap">[\s\S]*?<\/script>/,'').replace('href="./docs/FACTS.html"','href="#facts-inline"').replace('</footer>',()=>'</footer>'+info).replace('<script type="module" src="./src/app.js"></script>',()=>'<script>'+js.replaceAll('</script','<\\/script')+'</script>');
assert.ok(!single.includes('type="importmap"'));assert.ok(!single.includes('type="module" src='));assert.ok(single.includes('worldCanvas'));
const file=path.join(output,'SwitchBot-Living-World-3D.html');fs.writeFileSync(file,single);fs.writeFileSync(path.join(output,'portable-manifest.json'),JSON.stringify({sourceSha:process.env.SOURCE_SHA||process.env.GITHUB_SHA,bundler:'esbuild 0.25.9',generatedAt:new Date().toISOString(),file:path.basename(file),bytes:Buffer.byteLength(single),sha256:createHash('sha256').update(single).digest('hex'),changes:['Inline application, vendored renderer and CSS without changing 3D scene.','Inline fact sources and MIT license.','On file:// only, share creates the portable plan document instead of an invalid hosted URL.']},null,2));console.log('Portable 3D file built:',Buffer.byteLength(single),'bytes');
